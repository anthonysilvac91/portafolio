{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-css": "node-sass --include-path scss root/styles/style.scss root/styles/style.css", 
    "watch-css": "nodemon -e scss -x \"npm run build-css\""
  },

  "dependencies": {
    "node-sass": "^8.0.0",
    "nodemon": "^2.0.22"
  }
}
